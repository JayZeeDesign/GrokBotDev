---
// Route stub — §4.2.2's header nav links here (`builder bot`), so the route must
// resolve. The real page (§4.3.14 + CP-092…CP-096) is built in M3.
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="plugin builder bot — grokbot.dev" description="plugin builder on grokbot.dev.">
  <section class="container-page py-24" data-pagefind-body>
    <p class="mb-3 font-mono text-sm text-text-muted">builder bot</p>
    <h1 class="display-track font-display text-3xl font-semibold text-text md:text-4xl">
      plugin builder bot
    </h1>
  </section>
</BaseLayout>
